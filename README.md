# Mini-Operating-System-Kernel
A minimal operating system kernel built in C and Cpp that demonstrates core OS concepts including process scheduling, context switching, and memory management. The project simulates how an OS manages CPU resources, handles system calls, and ensures isolation between processes.  
